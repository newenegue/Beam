<h1><%= @album.title %></h1><br>
<% if @album.images != [] %>
	<ul>
		<% @album.images.each do |image| %>
			<li><%= image %></li>
		<% end %>
	</ul>
<% end %>

<br>

<%= link_to "Edit album", edit_album_path(@album) %>